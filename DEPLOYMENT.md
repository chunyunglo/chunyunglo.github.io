# GitHub Pages 部署指南

## 📋 部署前準備

### 1. 確認檔案結構
確保以下檔案都已正確設置：
- ✅ `.github/workflows/hugo.yml` - GitHub Actions 工作流程
- ✅ `static/css/custom.css` - 自定義樣式
- ✅ `static/js/custom.js` - 自定義 JavaScript
- ✅ `layouts/partials/navigators/lang-selector.html` - 語言選擇器（無國旗）
- ✅ `hugo.yaml` - Hugo 配置文件

### 2. 檢查 Hugo 配置

確認 `hugo.yaml` 中的 `baseURL` 設定正確：

```yaml
baseURL: https://你的GitHub用戶名.github.io/
```

如果你的倉庫名稱不是 `你的GitHub用戶名.github.io`，則需要設定為：

```yaml
baseURL: https://你的GitHub用戶名.github.io/倉庫名稱/
```

---

## 🚀 部署步驟

### 步驟 1：創建 GitHub 倉庫

1. 前往 GitHub 創建新倉庫
2. 倉庫名稱建議：`你的GitHub用戶名.github.io`（這樣網址會更簡潔）
3. 設定為 **Public**（GitHub Pages 免費版需要公開倉庫）

### 步驟 2：推送程式碼到 GitHub

```bash
# 初始化 Git（如果還沒有）
git init

# 添加所有檔案
git add .

# 提交
git commit -m "Initial commit: Hugo website with warm Morandi colors"

# 添加遠端倉庫（替換成你的倉庫 URL）
git remote add origin https://github.com/你的用戶名/你的倉庫名.git

# 推送到 main 分支
git branch -M main
git push -u origin main
```

### 步驟 3：啟用 GitHub Pages

1. 前往你的 GitHub 倉庫
2. 點擊 **Settings**（設定）
3. 在左側選單找到 **Pages**
4. 在 **Source** 下拉選單中選擇 **GitHub Actions**
5. 儲存設定

### 步驟 4：等待部署完成

1. 前往 **Actions** 標籤頁
2. 你會看到 "Deploy Hugo site to GitHub Pages" 工作流程正在執行
3. 等待綠色勾勾出現（通常需要 2-5 分鐘）
4. 部署完成後，你的網站會在 `https://你的用戶名.github.io/` 上線

---

## 🔄 後續更新流程

每次修改網站內容後：

```bash
# 添加修改的檔案
git add .

# 提交修改
git commit -m "描述你的修改內容"

# 推送到 GitHub
git push
```

推送後，GitHub Actions 會自動重新建置和部署你的網站！

---

## 🎨 顏色配置

所有顏色配置都在 `static/css/custom.css` 檔案的開頭：

```css
:root {
    --primary-color: #DDBCB7;        /* 修改這裡改變主色調 */
    --secondary-color: #E8D3C0;      /* 修改這裡改變次要色 */
    --accent-color: #F4BAAF;         /* 修改這裡改變強調色 */
    --background-color: #F0DED4;     /* 修改這裡改變背景色 */
    --text-color: #99857E;           /* 修改這裡改變文字顏色 */
    --text-dark: #6B5B54;            /* 修改這裡改變深色文字 */
    --warm-beige: #BEB1A8;
    --soft-pink: #ECCED0;
    --earth-tone: #A79A89;
}
```

修改後記得：
1. 儲存檔案
2. `git add static/css/custom.css`
3. `git commit -m "Update color scheme"`
4. `git push`

---

## 🖼️ 圖片優化建議

### 問題：首頁圖片畫質差

**原因**：目前的頭像圖片解析度只有 214x266 像素，在高解析度螢幕上會顯得模糊。

### 解決方案：

#### 方法 1：替換高解析度圖片（推薦）

1. 準備一張至少 **800x800 像素**的高品質頭像圖片
2. 將圖片命名為 `chunyung.png`
3. 替換 `assets/images/author/chunyung.png`
4. 推送到 GitHub

```bash
# 替換圖片後
git add assets/images/author/chunyung.png
git commit -m "Update profile image to higher resolution"
git push
```

#### 方法 2：使用外部圖床

如果你有高品質圖片託管在其他地方（如 Imgur、Cloudinary 等）：

1. 編輯 `data/zh-tw/author.yaml`
2. 將 `image: "images/author/chunyung.png"` 改為完整 URL
3. 例如：`image: "https://i.imgur.com/你的圖片ID.png"`

#### 建議的圖片規格：

- **格式**：PNG 或 WebP（WebP 更小更快）
- **尺寸**：800x800 到 1200x1200 像素
- **檔案大小**：盡量控制在 500KB 以下
- **背景**：建議使用透明背景或與網站配色協調的背景

#### 圖片優化工具：

線上工具：
- [TinyPNG](https://tinypng.com/) - 壓縮 PNG 圖片
- [Squoosh](https://squoosh.app/) - Google 的圖片優化工具
- [Compressor.io](https://compressor.io/) - 多格式圖片壓縮

---

## 🐛 常見問題排除

### 問題 1：網站沒有顯示自定義顏色

**解決方案**：
1. 檢查 GitHub Actions 是否成功執行
2. 清除瀏覽器快取（Ctrl+Shift+R 或 Cmd+Shift+R）
3. 確認 `static/css/custom.css` 已正確推送到 GitHub

### 問題 2：圖片無法顯示

**解決方案**：
1. 確認圖片路徑正確
2. 檢查圖片檔案是否已推送到 GitHub
3. 確認 `baseURL` 設定正確

### 問題 3：部署失敗

**解決方案**：
1. 前往 GitHub Actions 查看錯誤訊息
2. 確認 Hugo 版本相容性
3. 檢查 `hugo.yaml` 語法是否正確

---

## 📝 檢查清單

部署前請確認：

- [ ] `baseURL` 設定正確
- [ ] 所有檔案已提交到 Git
- [ ] GitHub 倉庫設定為 Public
- [ ] GitHub Pages 設定為使用 GitHub Actions
- [ ] 高解析度頭像圖片已準備好
- [ ] 顏色配置符合預期

---

## 🔗 相關連結

- [Hugo 官方文檔](https://gohugo.io/documentation/)
- [GitHub Pages 文檔](https://docs.github.com/en/pages)
- [Toha 主題文檔](https://toha-guides.netlify.app/)

---

## 💡 進階技巧

### 自定義域名

如果你有自己的域名：

1. 在倉庫根目錄創建 `static/CNAME` 檔案
2. 內容為你的域名，例如：`www.你的域名.com`
3. 在域名提供商設定 DNS：
   - 添加 CNAME 記錄指向 `你的用戶名.github.io`
4. 在 GitHub Pages 設定中輸入你的自定義域名

### 效能優化

1. **啟用 CDN**：GitHub Pages 已內建 CDN
2. **圖片懶加載**：考慮使用 Hugo 的圖片處理功能
3. **最小化 CSS/JS**：已在 GitHub Actions 中啟用 `--minify`

---

需要幫助？查看 `COLOR_GUIDE.md` 了解詳細的顏色配置說明！
